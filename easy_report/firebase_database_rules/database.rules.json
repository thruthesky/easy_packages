{
  "rules": {
    "reports": {
      ".read": "auth.uid !== null",
      "$reportKey": {
        ".write": "newData.child('reporter').val() === auth.uid"
      },
      ".indexOn": [
        "reporter",
        "reportee",
        "path"
      ]
    }
  }
}