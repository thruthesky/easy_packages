name: easy_like
description: "An esay way to handle the like and dislike feature."
version: 0.0.1
homepage: https://github.com/thruthesky/easy_frame/tree/main/easy_like

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
